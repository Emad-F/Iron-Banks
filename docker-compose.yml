services:
  postgres:
    image: postgres:16
    container_name: acid-postgres
    environment:
      POSTGRES_DB: acid_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
